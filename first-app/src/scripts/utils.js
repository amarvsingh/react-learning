// Type Module needs to be very impprtant in script tag in HTML file for a Vanilla JS app

//Default just needs to be one (gets caught automatically)
export default "abcd-efg-hij";

export let apiKey = "hdvvfc-123e3-adbad";
export let timeout = 30;
